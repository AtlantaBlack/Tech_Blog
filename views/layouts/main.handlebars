<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/css/normalize.css">
  <link rel="stylesheet" href="/css/skeleton.css">
  <link rel="stylesheet" href="/css/style.css">
  <title>The Tech Blog</title>
</head>

<body>
  <header>
    <h1>The Tech Blog</h1>
    <nav>
      <ul>
        <li class="inline-block">
          <a class="button custom-a" href="/">Home</a>
        </li>
        <li class="inline-block">
          <a class="button custom-a" href="/dashboard">Dashboard</a>
        </li>

        {{#if logged_in}}
        {{!-- show logout button if logged in --}}
        <li class="inline-block">
          <button id="logout" class="custom-a">Logout</a>
        </li>
        {{else}}
        {{!-- show login button otherwise --}}
        <li class="inline-block">
          <a class="button custom-a" href="/login">Login</a>
        </li>
        {{/if}}
      </ul>
    </nav>
  </header>

  <main>
    {{{ body }}}
  </main>

  <footer>
    &copy; 2022 Sushan Yue
  </footer>

  {{!-- logged in/logged out script --}}
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>